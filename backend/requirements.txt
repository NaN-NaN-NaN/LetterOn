# LetterOn Server - Python Dependencies
# Purpose: All required Python packages for the FastAPI backend
# Testing: pip install -r requirements.txt
# AWS Deployment: These dependencies are installed in Docker container

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6  # For file uploads

# AWS SDK
boto3==1.34.28
botocore==1.34.28

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.1.2

# Background Tasks & Scheduling
apscheduler==3.10.4  # Reminder scheduler

# Utilities
python-dotenv==1.0.0  # Environment variable management
email-validator==2.1.0  # Email validation

# HTTP Client (for calling external APIs if needed)
httpx==0.26.0

# Logging
python-json-logger==2.0.7  # Structured JSON logging

# Date/Time utilities
python-dateutil==2.8.2

# Development & Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
moto[dynamodb]==5.0.0  # Mock AWS services for testing
httpx==0.26.0  # For FastAPI TestClient

# Optional: Deployment helpers
mangum==0.17.0  # For AWS Lambda deployment (optional)
gunicorn==21.2.0  # Production WSGI server alternative

# Type checking (optional, for development)
mypy==1.8.0
types-python-dateutil==2.8.19.20240106
